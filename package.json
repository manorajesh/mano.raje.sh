{
  "name": "mano.raje.sh",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "astro": "astro",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-09-09 --proxy 3000 -- astro dev",
    "pages:deploy": "astro build && wrangler pages deploy ./dist",
    "pretty": "prettier --write \"./**/*.{js,jsx,mjs,cjs,ts,tsx,json}\""
  },
  "dependencies": {
    "@astrojs/cloudflare": "^7.0.2",
    "@astrojs/mdx": "^1.0.3",
    "astro": "^3.0.12"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "wrangler": "^3.7.0"
  }
}
